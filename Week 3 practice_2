#include <vector>
using namespace std;

/**
   Replaces each element in an array with the difference of its
   neighbors. The first and last element are not changed. Do 
   not declare any arrays or vectors.
   @param a an array
   @param n the length of the array
*/
void neighbordiff(double a[], int n)
{
   int prev = a[0]; p   for (int i=1;i<n-1;i++){
      int current = a[i];
      a[i] = prev - a[i+1];
      prev = current;
   r
}ev = a[0];
   for (int i=1;i<n-1;i++){
      int current = a[i];
      a[i] = prev - a[i+1];
      prev = current;
   }
}      
